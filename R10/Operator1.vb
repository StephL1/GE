Public Class Operator1

   

    Private Sub Operator1_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load

    End Sub

    Private Sub Button1_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button1.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button1.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button1.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()

        Me.Close()

    End Sub

    Private Sub Button2_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button2.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button2.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button2.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()

    End Sub

    Private Sub Button3_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button3.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button3.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button3.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button4_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button4.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button4.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button4.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button5_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button5.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button5.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button5.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button6_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button6.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button6.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button6.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button7_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button7.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button7.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button7.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub

    Private Sub Button8_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button8.Click

        If people_flag = "A" Then
            auditor_id(whichln) = Button8.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button8.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button9_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button9.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button9.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button9.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button10_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button10.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button10.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button10.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()

        Me.Close()
    End Sub
    Private Sub Button11_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button11.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button11.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button11.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub


    Private Sub Button12_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button12.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button12.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button12.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button13_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button13.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button13.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button13.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button14_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button14.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button14.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button14.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button15_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button15.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button15.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button15.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button16_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button16.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button16.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button16.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button17_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button17.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button17.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button17.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button18_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button18.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button18.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button18.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button19_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button19.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button19.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button19.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button20_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button20.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button20.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button20.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button21_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button21.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button21.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button21.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button22_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button22.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button22.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button22.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button23_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button23.Click
        If people_flag = "A" Then
            auditor_id(whichln) = Button23.Text
            R10.btnAuditor.Text = "Auditor " + auditor_id(whichln)
        Else
            moperator(whichln) = Button23.Text
            R10.btnOperator.Text = "Operator " + moperator(whichln)
        End If
        write_parameters()
        Me.Close()
    End Sub
    Private Sub Button24_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button24.Click
        alphabet.Show()
        Me.Close()
    End Sub

 

End Class